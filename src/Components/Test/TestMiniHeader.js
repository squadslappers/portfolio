import React from 'react';

const TestMiniHeader = (props) => {
  return(<div className='mini-header' id='blue-header'>
    <h1>HI, I'M JAKE AND I'M A {props.title}</h1>
  </div>)
}
export default TestMiniHeader;
